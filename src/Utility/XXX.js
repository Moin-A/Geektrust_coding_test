import React from "react";
import UseDropdown from "../CustomHooks/UseDropdown";
import Input from "../Components/Input";

const XXX = ({ index }) => {
  return (
    <React.Fragment>
      <UseDropdown index={index} />
      <Input />
    </React.Fragment>
  );
};

export default XXX;
